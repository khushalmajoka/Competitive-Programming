#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, k, arr[n];
    cin >> n ;
    int i = 0;
    while(n > 0 && i < n){
        cin >> arr[i++];
        n--;
    }
    cin >> k;
    for(int i =0; i < n; i ++){
        e;
        cout << i;
    }
    

    return 0;
}